# AB Extraction ESX

Free ESX Legacy extraction-RP foundation inspired by extraction shooter mechanics.

## Install
Import `sql/install.sql`, place the resource in `resources/[local]/ab_extraction`, and add `ensure ab_extraction` after `oxmysql` and `es_extended`.

Commands: `/inventory`, `/solo`, `/duo`, `/squad`, `/extract`.
CREATE TABLE IF NOT EXISTS ab_characters (char_identifier VARCHAR(120) PRIMARY KEY, inventory LONGTEXT NOT NULL, stash LONGTEXT NOT NULL, level INT NOT NULL DEFAULT 1, xp INT NOT NULL DEFAULT 0, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);